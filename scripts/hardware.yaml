session_name: Controller
windows:
  - layout: tiled
    panes:
      - shell_command:
        - roslaunch custom_description unitree_hw_only.launch
      - shell_command:
        - sleep 1
        - roslaunch legged_deployment mocap.launch   
      - shell_command:
        - sleep 5
        - roslaunch legged_deployment load_controller.launch
      - shell_command:
        - sleep 10
        - rosrun legged_deployment controller_client_node perceptive_controller start
      - shell_command:
        - sleep 10
        - roslaunch legged_deployment elevation_mapping_fixed.launch rviz:=true world_name:=step
      - shell_command:
        - sleep 2
        - rosrun legged_deployment bagfile_recorder
      - shell_command:
        - sleep 12
        - rosrun teleop_twist_keyboard teleop_twist_keyboard.py
    shell_command_before:
      - source ~/Projects/ocs2_ws/devel/setup.bash 
      - export ROBOT_TYPE=aliengo
    window_name: Controller